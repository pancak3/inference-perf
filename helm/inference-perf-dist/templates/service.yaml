{{- if .Values.service.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "inference-perf-dist.fullname" . }}
  labels:
    {{- include "inference-perf-dist.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "inference-perf-dist.selectorLabels" . | nindent 4 }}
{{- end }}
