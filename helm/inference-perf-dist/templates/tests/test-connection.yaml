apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "inference-perf-dist.fullname" . }}-test-connection"
  labels:
    {{- include "inference-perf-dist.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "inference-perf-dist.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
